<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs', {title: 'Nomad Nest'}) %>
    <link rel="stylesheet" href="/styles/home.css">
</head>

<body>
    <%- include('./partials/nav.ejs') %>

    <main>
        <section class="hero">
           
            <h2>Find Your Home Away from Home</h2>
            <a href="/destinations" class="btn">Let's Go!</a>
        </section>

        <section class="featured-destinations">
            <h2>Best Voted by Digital Nomads</h2>
            <ul class="destination-grid">
                <% for (let destination of destinations) { %>
                <li>
                    <figure>
                        <img src="<%= destination.photo %>">
                    </figure>
                    <figcaption>
                        <div class="featured-info">
                            <h3><%= destination.name %></h3>
                            <p>Average Monthly Cost: <span style="color:black"><%= destination.averageCost %></span></p>
                            <p>Visa Requirement: <span style="color:black"><%= destination.visa %></span></p>
                        </div>
                        <a href="/destinations/<%= destination._id %>" class="btn">Explore</a>
                    </figcaption>
                </li>
                <% } %>
            </ul>
        </section>
    </main>

    <%- include('./partials/footer.ejs') %>
</body>

</html>
